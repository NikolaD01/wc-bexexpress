{% set class = class is defined ? class : '' %}
{% set text = text is defined ? text : 'Save' %}

<div>
    <p class="submit">
        <input type="submit" name="submit" id="submit" class="button button-primary {{ class }}" value="{{ text }}">
    </p>
</div>
